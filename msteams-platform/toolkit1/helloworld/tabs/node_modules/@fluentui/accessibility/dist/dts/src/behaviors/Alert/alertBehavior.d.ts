import { Accessibility } from '../../types';
/**
 * @description
 * Uses `alertWarningBehavior` for 'danger' and 'warning' variants.
 */
export declare const alertBehavior: Accessibility<AlertBehaviorProps>;
export declare type AlertBehaviorProps = {
    warning?: boolean;
    danger?: boolean;
    bodyId?: string;
};
