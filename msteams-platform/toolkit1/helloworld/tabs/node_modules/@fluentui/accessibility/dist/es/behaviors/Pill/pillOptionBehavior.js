import { keyboardKey } from '../../keyboard-key';
import { IS_FOCUSABLE_ATTRIBUTE } from '../../attributes';
export var pillOptionBehavior = function pillOptionBehavior(p) {
  var _root;

  return {
    attributes: {
      root: (_root = {
        role: 'option'
      }, _root[IS_FOCUSABLE_ATTRIBUTE] = true, _root)
    },
    keyActions: {
      root: {
        performDismiss: {
          keyCombinations: [{
            keyCode: keyboardKey.Delete
          }, {
            keyCode: keyboardKey.Backspace
          }]
        }
      }
    }
  };
};
//# sourceMappingURL=pillOptionBehavior.js.map
