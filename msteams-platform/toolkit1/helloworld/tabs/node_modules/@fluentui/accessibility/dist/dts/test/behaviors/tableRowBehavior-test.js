"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var accessibility_1 = require("@fluentui/accessibility");
describe('tableRowBehavior.ts', function () {
    test('use tableHeaderCellBehavior if table row has header prop defined', function () {
        var props = {
            header: true,
        };
        var expectedResult = accessibility_1.tableRowBehavior(props);
        expect(expectedResult.childBehaviors.cell.name).toEqual('tableHeaderCellBehavior');
    });
    test('use tableCellBehavior if table row has NOT header prop defined', function () {
        var props = {};
        var expectedResult = accessibility_1.tableRowBehavior(props);
        expect(expectedResult.childBehaviors.cell.name).toEqual('tableCellBehavior');
    });
});
