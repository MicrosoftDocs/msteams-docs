"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var accessibility_1 = require("@fluentui/accessibility");
var mockedMenuId = 'anyMenuId';
describe('MenuButtonBehavior.ts', function () {
    test('aria-controls are NOT defined, when menu is NOT open', function () {
        var property = {
            open: false,
            menuId: mockedMenuId,
        };
        var expectedResult = accessibility_1.menuButtonBehavior(property);
        expect(expectedResult.attributes.trigger['aria-controls']).toBe(undefined);
    });
    test('aria-controls are defined, when menu is open', function () {
        var property = {
            open: true,
            menuId: mockedMenuId,
        };
        var expectedResult = accessibility_1.menuButtonBehavior(property);
        expect(expectedResult.attributes.trigger['aria-controls']).toBe(mockedMenuId);
    });
});
