"use strict";

exports.__esModule = true;
exports.toolbarBehavior = void 0;

var _types = require("../../focusZone/types");

/**
 * @description
 * Implements ARIA Toolbar design pattern.
 * Child item components need to have toolbarItemBehavior assigned.
 * @specification
 * Adds role 'toolbar' to 'root' slot.
 * Provides arrow key navigation in horizontal direction.
 * When component's container element receives focus, focus will be set to the default focusable child element of the component.
 */
var toolbarBehavior = function toolbarBehavior() {
  return {
    attributes: {
      root: {
        role: 'toolbar'
      }
    },
    focusZone: {
      props: {
        shouldFocusInnerElementWhenReceivedFocus: true,
        direction: _types.FocusZoneDirection.horizontal
      }
    }
  };
};

exports.toolbarBehavior = toolbarBehavior;
//# sourceMappingURL=toolbarBehavior.js.map
