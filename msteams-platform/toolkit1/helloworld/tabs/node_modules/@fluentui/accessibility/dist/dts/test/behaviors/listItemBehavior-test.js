"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var accessibility_1 = require("@fluentui/accessibility");
describe('ListItemBehavior.ts', function () {
    test('use SelectableListItemBehavior if selectable prop is defined', function () {
        var property = {
            selectable: true,
        };
        var expectedResult = accessibility_1.listItemBehavior(property);
        expect(expectedResult.attributes.root.role).toEqual('option');
    });
    test('use NavigableListItemBehavior if navigable prop is defined', function () {
        var property = {
            navigable: true,
        };
        var expectedResult = accessibility_1.listItemBehavior(property);
        expect(expectedResult.attributes.root.role).toEqual('menuitem');
        expect(expectedResult.attributes.root[accessibility_1.IS_FOCUSABLE_ATTRIBUTE]).toEqual(true);
    });
    test('use BasicListBehavior if selectable prop is NOT defined', function () {
        var property = {};
        var expectedResult = accessibility_1.listItemBehavior(property);
        expect(expectedResult.attributes.root.role).toEqual('listitem');
    });
});
