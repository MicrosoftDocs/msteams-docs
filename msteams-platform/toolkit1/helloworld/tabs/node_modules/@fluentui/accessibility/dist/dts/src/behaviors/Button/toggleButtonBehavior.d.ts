import { Accessibility } from '../../types';
import { ButtonBehaviorProps } from './buttonBehavior';
export declare const toggleButtonBehavior: Accessibility<ToggleButtonBehaviorProps>;
declare type ToggleButtonBehaviorProps = ButtonBehaviorProps & {
    /** Indicates if a button is in pressed state. */
    active: boolean;
};
export {};
