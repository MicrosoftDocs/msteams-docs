"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.submenuBehavior = void 0;
var types_1 = require("../../focusZone/types");
var menuItemBehavior_1 = require("./menuItemBehavior");
/**
 * @description
 * The 'menu' role is used to identify an element that creates a list of common actions or functions that a user can invoke.
 *
 * @specification
 * Adds role='menu'.
 * Provides arrow key navigation in vertical direction.
 * Keyboard navigation is circular.
 * Component will get focus when mounted.
 */
var submenuBehavior = function () { return ({
    attributes: {
        root: {
            role: 'menu',
        },
    },
    focusZone: {
        props: {
            isCircularNavigation: true,
            shouldFocusOnMount: true,
            direction: types_1.FocusZoneDirection.vertical,
        },
    },
    childBehaviors: { item: menuItemBehavior_1.menuItemBehavior },
}); };
exports.submenuBehavior = submenuBehavior;
