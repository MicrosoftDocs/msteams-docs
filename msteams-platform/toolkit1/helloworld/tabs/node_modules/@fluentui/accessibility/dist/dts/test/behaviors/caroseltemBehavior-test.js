"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var accessibility_1 = require("@fluentui/accessibility");
describe('carouselItemBehavior.ts', function () {
    test('sets tabIndex="0" on root when carousel has navigation and item is visible ', function () {
        var expectedResult = accessibility_1.carouselItemBehavior({ navigation: true, active: true });
        expect(expectedResult.attributes.root.tabIndex).toEqual(0);
    });
    test('sets tabIndex="-1" on root when carousel has navigation and item is NOT visible ', function () {
        var expectedResult = accessibility_1.carouselItemBehavior({ navigation: true, active: false });
        expect(expectedResult.attributes.root.tabIndex).toEqual(-1);
    });
    test('do NOT set tabIndex on root when carousel has NO navigation', function () {
        var expectedResult = accessibility_1.carouselItemBehavior({ navigation: false, active: true });
        expect(expectedResult.attributes.root.tabIndex).toBeUndefined;
    });
});
