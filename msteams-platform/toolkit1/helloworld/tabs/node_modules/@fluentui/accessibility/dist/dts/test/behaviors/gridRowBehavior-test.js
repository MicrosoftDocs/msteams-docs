"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var accessibility_1 = require("@fluentui/accessibility");
describe('gridRowBehavior.ts', function () {
    test('use gridHeaderRowBehavior if grid row has header prop defined', function () {
        var props = {
            header: true,
        };
        var expectedResult = accessibility_1.gridRowBehavior(props);
        expect(expectedResult.childBehaviors.cell.name).toEqual('gridHeaderCellBehavior');
    });
    test('use gridRowNestedBehavior if grid row has NOT header prop defined', function () {
        var props = {};
        var expectedResult = accessibility_1.gridRowBehavior(props);
        expect(expectedResult.childBehaviors.cell.name).toEqual('gridCellBehavior');
    });
});
