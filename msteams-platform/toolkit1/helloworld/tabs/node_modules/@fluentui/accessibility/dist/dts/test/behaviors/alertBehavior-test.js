"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var accessibility_1 = require("@fluentui/accessibility");
describe('AlertBehavior.ts', function () {
    test('use alertWarningBehavior if warning prop is defined', function () {
        var expectedResult = accessibility_1.alertBehavior({ warning: true });
        expect(expectedResult.attributes.body.role).toEqual('alert');
    });
    test('use alertWarningBehavior if danger prop is defined', function () {
        var expectedResult = accessibility_1.alertBehavior({ danger: true });
        expect(expectedResult.attributes.body.role).toEqual('alert');
    });
    test('use aria-describedby if dismiss action is defined for non-warning alert', function () {
        var expectedResult = accessibility_1.alertBehavior({ bodyId: 'alertId' });
        expect(expectedResult.attributes.dismissAction['aria-describedby']).toEqual('alertId');
    });
    test('use aria-describedby if dismiss action is defined for warning alert', function () {
        var expectedResult = accessibility_1.alertBehavior({ warning: true, bodyId: 'alertId' });
        expect(expectedResult.attributes.dismissAction['aria-describedby']).toEqual('alertId');
    });
});
