import { FocusZoneDirection } from '../../focusZone/types';
/**
 * @description
 * Behavior for a datepicker calendar grid.
 * @specification
 * Provides arrow key navigation in bidirectional direction.
 * Adds role='grid'.
 */

export var datepickerCalendarGridBehavior = function datepickerCalendarGridBehavior() {
  return {
    attributes: {
      root: {
        role: 'grid'
      }
    },
    focusZone: {
      props: {
        direction: FocusZoneDirection.bidirectional,
        pagingSupportDisabled: true
      }
    }
  };
};
//# sourceMappingURL=datepickerCalendarGridBehavior.js.map
