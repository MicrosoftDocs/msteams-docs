import { Accessibility } from '../../types';
export declare const buttonBehavior: Accessibility<ButtonBehaviorProps>;
export declare type ButtonBehaviorProps = {
    /** Element type. */
    as: string;
    /** A button can show it is currently unable to be interacted with. */
    disabled?: boolean;
    disabledFocusable?: boolean;
};
