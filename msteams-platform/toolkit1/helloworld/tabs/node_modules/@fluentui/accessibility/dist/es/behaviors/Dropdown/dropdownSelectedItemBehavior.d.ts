import { Accessibility } from '../../types';
/**
 * @description
 * Add tabIndex to 0 if active and -1 if not
 * @specification
 * Adds role='option'.
 * Adds attribute 'aria-hidden=true' to 'icon' slot.
 */
export declare const dropdownSelectedItemBehavior: Accessibility<DropdownSelectedItemBehaviorProps>;
export declare type DropdownSelectedItemBehaviorProps = {
    header: string;
    active: boolean;
};
