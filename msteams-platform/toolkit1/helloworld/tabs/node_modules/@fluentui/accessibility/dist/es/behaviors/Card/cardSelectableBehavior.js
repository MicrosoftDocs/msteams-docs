import { SpacebarKey } from '../../keyboard-key';
import { IS_FOCUSABLE_ATTRIBUTE } from '../../attributes';
/**
 * @description
 * Behavior for a selectable card component
 * @specification
 * Adds role='group'.
 * Adds attribute 'aria-disabled=true' based on the property 'disabled'.
 * Adds attribute 'tabIndex=0' to 'root' slot.
 * Adds attribute 'data-is-focusable=true' to 'root' slot.
 * Triggers 'performClick' action with 'Spacebar' on 'root'.
 */

export var cardSelectableBehavior = function cardSelectableBehavior(props) {
  var _root;

  return {
    attributes: {
      root: (_root = {
        role: 'group',
        tabIndex: 0
      }, _root[IS_FOCUSABLE_ATTRIBUTE] = true, _root['aria-disabled'] = props.disabled, _root)
    },
    keyActions: {
      root: {
        performClick: {
          keyCombinations: [{
            keyCode: SpacebarKey
          }]
        }
      }
    }
  };
};
//# sourceMappingURL=cardSelectableBehavior.js.map
