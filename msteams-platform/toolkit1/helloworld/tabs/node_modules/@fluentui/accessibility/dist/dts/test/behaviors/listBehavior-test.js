"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var accessibility_1 = require("@fluentui/accessibility");
describe('ListBehavior.ts', function () {
    test('use SelectableListBehavior if selectable prop is defined', function () {
        var property = {
            selectable: true,
        };
        var expectedResult = accessibility_1.listBehavior(property);
        expect(expectedResult.attributes.root.role).toEqual('listbox');
    });
    test('use NavigableListBehavior if navigable prop is defined', function () {
        var property = {
            navigable: true,
        };
        var expectedResult = accessibility_1.listBehavior(property);
        expect(expectedResult.attributes.root.role).toEqual('menu');
        expect(expectedResult.focusZone).toBeTruthy();
    });
    test('use BasicListItemBehavior if selectable prop is NOT defined', function () {
        var property = {};
        var expectedResult = accessibility_1.listBehavior(property);
        expect(expectedResult.attributes.root.role).toEqual('list');
    });
});
