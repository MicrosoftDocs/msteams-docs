import _merge from "lodash/merge";
import { treeTitleBehavior } from './treeTitleBehavior';
/**
 * @description
 * Adds role 'listitem' if the title is a leaf node inside the tree.
 */

export var treeTitleAsListItemTitleBehavior = function treeTitleAsListItemTitleBehavior(props) {
  var behavior = treeTitleBehavior(props);

  var definition = _merge(behavior, {
    attributes: {
      root: Object.assign({}, !props.hasSubtree && {
        role: 'listitem'
      })
    }
  });

  if (process.env.NODE_ENV !== 'production') {
    if (!props.hasSubtree) definition.attributes.root['data-aa-class'] = 'TreeTitleList';else definition.attributes.root['data-aa-class'] = behavior.attributes.root['data-aa-class'];
  }

  return definition;
};
//# sourceMappingURL=treeTitleAsListItemTitleBehavior.js.map
