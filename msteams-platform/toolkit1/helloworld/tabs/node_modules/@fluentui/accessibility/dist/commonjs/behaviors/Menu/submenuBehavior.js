"use strict";

exports.__esModule = true;
exports.submenuBehavior = void 0;

var _types = require("../../focusZone/types");

var _menuItemBehavior = require("./menuItemBehavior");

/**
 * @description
 * The 'menu' role is used to identify an element that creates a list of common actions or functions that a user can invoke.
 *
 * @specification
 * Adds role='menu'.
 * Provides arrow key navigation in vertical direction.
 * Keyboard navigation is circular.
 * Component will get focus when mounted.
 */
var submenuBehavior = function submenuBehavior() {
  return {
    attributes: {
      root: {
        role: 'menu'
      }
    },
    focusZone: {
      props: {
        isCircularNavigation: true,
        shouldFocusOnMount: true,
        direction: _types.FocusZoneDirection.vertical
      }
    },
    childBehaviors: {
      item: _menuItemBehavior.menuItemBehavior
    }
  };
};

exports.submenuBehavior = submenuBehavior;
//# sourceMappingURL=submenuBehavior.js.map
