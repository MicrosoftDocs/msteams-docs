"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var accessibility_1 = require("@fluentui/accessibility");
describe('treeItemAsOptionBehavior', function () {
    describe('role', function () {
        test("is 'option' when item hasSubtree", function () {
            var expectedResult = accessibility_1.treeItemAsOptionBehavior({ hasSubtree: true });
            expect(expectedResult.attributes.root.role).toEqual('option');
        });
    });
    describe('aria-selected', function () {
        test("is added with 'true' value to an item hasSubtree, is selectable and selected", function () {
            var expectedResult = accessibility_1.treeItemAsOptionBehavior({ hasSubtree: true, selectable: true, selected: true });
            expect(expectedResult.attributes.root['aria-selected']).toEqual(true);
        });
        test("is added with 'false' value to an item hasSubtree, is selectable and not selected", function () {
            var expectedResult = accessibility_1.treeItemAsOptionBehavior({ hasSubtree: true, selectable: true });
            expect(expectedResult.attributes.root['aria-selected']).toEqual(false);
        });
        test("is not added to an item that is not selectable ", function () {
            var expectedResult = accessibility_1.treeItemAsOptionBehavior({ hasSubtree: true });
            expect(expectedResult.attributes.root['aria-selected']).toBeUndefined();
        });
    });
});
