import * as React from 'react';
import { NestingContext } from '../NestingContext';
import { useNestingChild } from './useNestingChild';
import { useNestingRoot } from './useNestingRoot';
// These hooks are not used currently

/* eslint-disable */
export var useNestingAuto = function useNestingAuto() {
  var context = React.useContext(NestingContext);
  return context ? useNestingChild() : useNestingRoot();
};
//# sourceMappingURL=useNestingAuto.js.map
