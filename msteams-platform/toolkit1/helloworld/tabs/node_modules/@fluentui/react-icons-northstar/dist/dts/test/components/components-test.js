"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var icons = require("@fluentui/react-icons-northstar");
var fs = require("fs");
var path = require("path");
var ReactIs = require("react-is");
var iconsDir = 'src/components';
describe('Icon components', function () {
    fs.readdirSync(iconsDir).forEach(function (file) {
        var componentName = path.basename(file, '.tsx');
        var IconComponent = icons[componentName];
        test(componentName + " has 'Icon' suffix in the name", function () {
            expect(componentName.endsWith('Icon')).toBeTruthy();
        });
        test(file + " is exported in package", function () {
            expect(IconComponent).toBeDefined();
        });
        test(file + " is a valid component", function () {
            expect(ReactIs.isValidElementType(IconComponent)).toEqual(true);
        });
        test(file + " matches displayName", function () {
            expect(IconComponent.displayName).toEqual(componentName);
        });
    });
});
