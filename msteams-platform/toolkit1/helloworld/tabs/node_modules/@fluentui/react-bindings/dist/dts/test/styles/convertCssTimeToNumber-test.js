"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var react_bindings_1 = require("@fluentui/react-bindings");
describe('convertCssTimeToNumber', function () {
    test('converts "ms" values to number', function () {
        expect(react_bindings_1.unstable_calculateAnimationTimeout('200ms', '300ms')).toEqual(500);
    });
    test('converts "s" values to number', function () {
        expect(react_bindings_1.unstable_calculateAnimationTimeout('20s', '2s')).toEqual(22000);
    });
    test('converts decimal values to number', function () {
        expect(react_bindings_1.unstable_calculateAnimationTimeout('2.5s', '2s')).toEqual(4500);
    });
    test('converts other valid css values to 0', function () {
        expect(react_bindings_1.unstable_calculateAnimationTimeout('initial', '200ms')).toEqual(0);
        expect(react_bindings_1.unstable_calculateAnimationTimeout('inherit', '400ms')).toEqual(0);
    });
    test('converts invalid css values to 0', function () {
        expect(react_bindings_1.unstable_calculateAnimationTimeout('intial', undefined)).toEqual(0);
    });
    test('returns the duration if delay is not set', function () {
        expect(react_bindings_1.unstable_calculateAnimationTimeout('400ms')).toEqual(400);
        expect(react_bindings_1.unstable_calculateAnimationTimeout('2s')).toEqual(2000);
    });
});
