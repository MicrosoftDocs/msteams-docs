import { AnyAction, SideEffect } from '@fluentui/state';
import * as React from 'react';
declare type Dispatch<Action extends AnyAction> = (e: DispatchEvent, action: Action, ...args: Parameters<Action>) => void;
declare type DispatchEffect<State> = (e: DispatchEvent, prevState: State, nextState: State) => void;
declare type DispatchEvent = React.SyntheticEvent | Event;
export declare const useDispatchEffect: <State>(dispatchEffect: DispatchEffect<State>) => [Dispatch<AnyAction>, SideEffect<State>];
export {};
