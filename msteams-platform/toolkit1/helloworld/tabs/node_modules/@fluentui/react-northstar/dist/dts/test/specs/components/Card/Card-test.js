"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var React = require("react");
var Card_1 = require("src/components/Card/Card");
var commonTests_1 = require("test/specs/commonTests");
var utils_1 = require("test/utils");
describe('Card', function () {
    commonTests_1.isConformant(Card_1.Card, { testPath: __filename, constructorName: 'Card' });
    describe('accessiblity', function () {
        commonTests_1.handlesAccessibility(Card_1.Card, { defaultRootRole: 'group' });
    });
    describe('onClick', function () {
        test('is called with (e, props) on a click', function () {
            var onClick = jest.fn();
            var card = utils_1.mountWithProviderAndGetComponent(Card_1.Card, React.createElement(Card_1.Card, { onClick: onClick }));
            card.simulate('click');
            expect(onClick).toHaveBeenCalledTimes(1);
            expect(onClick).toHaveBeenCalledWith(expect.objectContaining({ type: 'click' }), expect.objectContaining({ onClick: onClick }));
        });
        test('does not call onClick when the card is disabled', function () {
            var onClick = jest.fn();
            var card = utils_1.mountWithProviderAndGetComponent(Card_1.Card, React.createElement(Card_1.Card, { disabled: true, onClick: onClick }));
            card.simulate('click');
            expect(onClick).not.toHaveBeenCalled();
        });
    });
});
