"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var React = require("react");
var utils_1 = require("test/utils");
var TextArea_1 = require("src/components/TextArea/TextArea");
var commonTests_1 = require("test/specs/commonTests");
var faker = require("faker");
describe('TextArea', function () {
    commonTests_1.isConformant(TextArea_1.TextArea, { testPath: __filename, constructorName: 'TextArea', autoControlledProps: ['value'] });
    describe('defaultValue', function () {
        test('sets "defaultValue" as initial "value"', function () {
            var value = faker.lorem.words();
            var wrapper = utils_1.mountWithProvider(React.createElement(TextArea_1.TextArea, { defaultValue: value }));
            expect(wrapper.find('textarea').prop('value')).toBe(value);
        });
    });
    describe('onChange', function () {
        test('is called with (e, data)', function () {
            var value = faker.lorem.words();
            var onChange = jest.fn();
            var wrapper = utils_1.mountWithProvider(React.createElement(TextArea_1.TextArea, { onChange: onChange }));
            wrapper.simulate('change', { target: { value: value } });
            expect(onChange).toBeCalledTimes(1);
            expect(onChange).toBeCalledWith(expect.objectContaining({ type: 'change' }), expect.objectContaining({ value: value }));
        });
    });
});
