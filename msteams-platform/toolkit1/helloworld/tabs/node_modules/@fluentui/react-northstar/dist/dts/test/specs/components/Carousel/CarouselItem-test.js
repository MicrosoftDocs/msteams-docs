"use strict";
var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
Object.defineProperty(exports, "__esModule", { value: true });
var React = require("react");
var commonTests_1 = require("test/specs/commonTests");
var CarouselItem_1 = require("src/components/Carousel/CarouselItem");
var utils_1 = require("test/utils");
function renderCarouselItem(props) {
    return utils_1.mountWithProvider(React.createElement(CarouselItem_1.CarouselItem, __assign({}, props)));
}
var getItemPositionContainer = function (wrapper) {
    return utils_1.findIntrinsicElement(wrapper, "." + CarouselItem_1.carouselItemSlotClassNames.itemPositionText);
};
describe('CarouselItem', function () {
    commonTests_1.isConformant(CarouselItem_1.CarouselItem, { testPath: __filename, constructorName: 'CarouselItem' });
    it('itemPositionText is added inside the item along with the content', function () {
        var wrapper = renderCarouselItem({ itemPositionText: 'test-position' });
        var itemPositionContainer = getItemPositionContainer(wrapper);
        expect(itemPositionContainer.getDOMNode().textContent).toEqual('test-position');
    });
    it('id is replaced with the one passed as prop', function () {
        var wrapper = renderCarouselItem({ id: 'bla-bla' });
        expect(wrapper.getDOMNode().getAttribute('id')).toEqual('bla-bla');
    });
});
